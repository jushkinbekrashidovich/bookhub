import 'package:flutter/material.dart';

class AddBookReview extends StatelessWidget {
  const AddBookReview({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(

        
        children: [
          Container(

          ),
        ],
      ),
    );
  }
}